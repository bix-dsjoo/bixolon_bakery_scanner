# Deployment configuration

Deployment profiles bind a pipeline config to a platform, runtime lock,
artifact lock, package allowlist, and verification command. The Windows camera
installer remains under `deployment/camera_installer/`; portable legacy paths
remain unchanged for compatibility.
