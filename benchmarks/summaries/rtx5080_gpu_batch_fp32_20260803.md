# RTX 5080 batch FP32 evidence checkpoint — 2026-08-03

Status: `unverified_missing_artifact`

The required artifact preflight failed before measurement. The exact missing
artifact IDs are:

- `rfdetr_large_bakery_v1_checkpoint`
- `repvit_m1_15plus5_v1_checkpoint`
- `repvit_m1_15plus5_v1_prototypes`
- `dinov3_vits16_15plus5_v1_weights`
- `dinov3_vits16_15plus5_v1_support`
- `dinov3_vits16_15plus5_v1_local_bank`

No declared artifact that existed mismatched its locked byte size or SHA-256.
The required external `rtx5080_worker_p95_v1` E/M/H manifest was also absent,
so it has no Git-safe SHA-256 identity. The status remains
`unverified_missing_artifact` because the artifact preflight is the immediate
contract gate; the independent manifest failure is recorded as an additional
unverified boundary.

The grouped CUDA benchmark was not run. Therefore this checkpoint has no
timing summaries, object-count distribution, DINO rate, runtime identity, or
parity receipt, and it does not establish the 100ms target.

Fresh Python verification: `756 passed, 4 skipped, 15 deselected`. The skipped
model/CUDA/clean-install coverage remains unverified. Flutter was unavailable
on this host, so `flutter analyze` and the specified Flutter contract tests
remain unverified.

The approved design is linked to the committed compact receipt in
[`docs/superpowers/specs/2026-08-03-rtx5080-gpu-p95-100ms-design.md`](../../docs/superpowers/specs/2026-08-03-rtx5080-gpu-p95-100ms-design.md).
