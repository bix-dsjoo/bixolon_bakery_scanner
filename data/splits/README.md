# Split manifests

Store only stable sample IDs and their role here. Development, calibration, and
locked acceptance IDs must be disjoint. Any use of locked data for a decision
retires that lock and requires a newly versioned locked set.
